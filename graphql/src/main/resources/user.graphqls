# Define schema
type User {
    id: ID!
    name: String!
    age: Int!
}

input UserRequest {
    name: String!
    age: Int!
}

# Root
extend type Query {
    user(id: ID!): User
}

# Root
extend type Mutation {
    addUser(request: UserRequest!): ID!
    removeUser(id: ID!): Boolean
}
